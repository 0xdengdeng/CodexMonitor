# CodexMonitor 上线前 UAT 测试文档

## 文档信息

| 项目 | 内容 |
| --- | --- |
| 文档名称 | CodexMonitor 上线前 UAT 测试文档 |
| 测试类型 | 用户验收测试 / 上线前全功能回归 |
| 测试对象 | CodexMonitor 桌面应用 |
| 适用版本 | 待发布版本 |
| 测试环境 | UAT 环境 |
| 测试负责人 | 待填写 |
| 执行人 | 待填写 |
| 执行日期 | 待填写 |
| 构建号 | 待填写 |

## 测试目标

上线前 UAT 用于确认待发布版本满足真实用户工作流要求，并具备稳定发布条件。验收重点包括发布安装、OTA 更新、应用启动、workspace 管理、thread 执行、composer follow-up、图片附件、生图、Git 状态、settings 持久化、daemon/RPC parity、事件同步、异常恢复和重启恢复。

通过 UAT 后，产品、研发和测试团队应能确认：

- P0 主链路可以稳定完成。
- 待发布安装包和 OTA 更新链路可被真实用户完成。
- 用户可在真实 workspace 中创建和继续 Codex thread。
- 文本任务、生图任务和图片附件相关能力可正常执行和展示。
- Git、settings、thread 状态和生成图片记录在重启后保持一致。
- 失败、超时、权限不足、无效路径等异常不会导致主流程不可恢复。

## 测试范围

| 模块 | 验收内容 | 优先级 |
| --- | --- | --- |
| 发布安装与 OTA | 全新安装、旧版本升级、更新提示、下载安装、重启后版本确认、数据保留 | P0 |
| 应用启动 | 首次启动、重复启动、重启恢复、异常配置启动 | P0 |
| Workspace 管理 | 添加、切换、删除、重复路径、无效路径、权限不足路径 | P0 |
| Thread 管理 | 创建、继续、恢复、隐藏、父子层级、处理中状态保留 | P0 |
| Composer | 普通发送、Queue 模式、Steer 模式、发送中禁用态、失败后继续发送 | P0 |
| Agent 执行 | 文本任务执行、输出流、完成状态、失败状态、中断/继续 | P0 |
| 图片附件 | 文件选择、拖拽/粘贴、图片预览、随消息发送、历史展示 | P0 |
| 生图功能 | 文本生图、生成中状态、图片预览、资产保存、失败重试、历史恢复 | P0 |
| Git 状态 | 分支、未提交变更、diff、stage 状态、初始化提示 | P1 |
| Settings | 设置修改、保存、默认值、迁移、重启恢复 | P1 |
| Daemon/RPC | 本地 app 与 daemon 行为一致、RPC 返回稳定、远程 workspace 基本链路 | P1 |
| 事件同步 | thread 状态、输出流、错误事件、重连后状态恢复 | P1 |
| 异常恢复 | 网络失败、模型调用失败、命令失败、路径失效、进程异常退出 | P1 |
| UI 回归 | 空状态、加载态、错误态、关键页面布局、图片卡片展示 | P2 |

## 不在本轮范围

| 项目 | 说明 |
| --- | --- |
| 性能压测 | 仅执行基础稳定性观察，不做大规模并发压测 |
| 安全渗透 | 仅覆盖 UAT 必要的权限和敏感路径验证 |
| 非目标平台兼容 | 仅覆盖本次发布声明支持的平台和系统版本 |
| 生图编辑能力 | 若当前版本仅支持文生图，则不验收图生图、局部重绘、透明背景等编辑能力 |

## 准入标准

开始 UAT 前必须满足：

- UAT 环境已安装待发布构建。
- 发布安装包、签名产物、更新 manifest 和 release notes 已生成并可访问。
- 已准备一个当前线上旧版本客户端，用于验证 OTA 更新链路。
- 开发自测和基础系统测试已完成。
- P0 已知缺陷已修复或有明确结论。
- 测试 workspace、Git 仓库、账号、权限、网络和模型配置已准备完成。
- 生图相关服务、模型、计费或企业 AI 登录状态满足本版本使用条件。
- 测试人员已获得需求、变更说明和已知问题清单。

## 准出标准

本版本可进入上线流程需满足：

- P0 UAT 案例全部通过。
- 无 Blocker 或 Critical 缺陷遗留。
- P1 缺陷均有明确评估、规避方案或修复计划。
- 主链路不存在应用崩溃、白屏、数据丢失、状态卡死、历史丢失问题。
- 生图结果、图片附件、thread 历史、workspace 和 settings 在重启后可恢复。
- 产品或业务验收负责人完成上线确认。

## 测试环境与数据

| 类型 | 要求 |
| --- | --- |
| 操作系统 | 本次发布支持的 macOS 版本 |
| 应用版本 | 待发布构建 |
| 旧版本客户端 | 当前线上稳定版本，用于 OTA 更新验收 |
| OTA manifest | `https://qihang-ai.tos-cn-beijing.volces.com/codexmonitor/latest.json` |
| 发布产物目录 | `release-artifacts/` |
| 测试仓库 | 有效 Git 仓库，包含至少一个未提交改动 |
| Workspace 路径 | `/Users/test/project/demo-repo` |
| 无效路径 | `/Users/test/project/not-exist` |
| 文本任务 | `请读取当前项目结构，并总结主要文件用途` |
| Follow-up 任务 | `请检查当前 Git 状态，并说明未提交变更` |
| 图片附件 | PNG/JPEG/HEIC 各一张；包含一个带空格路径的图片文件 |
| 生图提示词 | `生成一张 CodexMonitor 全功能 UAT 测试流程图，中文，16:9，清晰专业` |
| 高复杂度生图提示词 | `生成一张包含 12 个测试模块、状态标记、验收标准的中文测试看板` |
| 空提示词 | 空字符串 |

## 缺陷等级

| 等级 | 定义 | 上线影响 |
| --- | --- | --- |
| Blocker | 应用无法启动、主链路不可用、数据丢失、无法恢复 | 阻断上线 |
| Critical | P0 功能失败，例如 thread 无法执行、生图不可用、重启后历史丢失 | 原则上阻断上线 |
| Major | P1 功能异常，但存在明确规避方案 | 需评估后决定 |
| Minor | 文案、低频 UI、非关键边界问题 | 可记录后续修复 |

## UAT 用例清单

| 案例编号 | 案例名称 | 模块 | 优先级 |
| --- | --- | --- | --- |
| UAT-REL-001 | 发布安装与 OTA 更新验收 | 发布 | P0 |
| UAT-FULL-001 | 全功能主链路验收 | 全链路 | P0 |
| UAT-WS-001 | Workspace 管理验收 | Workspace | P0 |
| UAT-THREAD-001 | Thread 创建、恢复与隐藏验收 | Thread | P0 |
| UAT-COMPOSER-001 | Composer 发送与 Follow-up 模式验收 | Composer | P0 |
| UAT-IMG-ATTACH-001 | 图片附件发送与历史展示验收 | 图片附件 | P0 |
| UAT-IMG-GEN-001 | 生图功能验收 | 生图 | P0 |
| UAT-GIT-001 | Git 状态展示验收 | Git | P1 |
| UAT-SET-001 | Settings 保存与重启恢复验收 | Settings | P1 |
| UAT-RPC-001 | Daemon/RPC parity 验收 | Daemon/RPC | P1 |
| UAT-ERR-001 | 异常恢复验收 | 异常恢复 | P1 |

## UAT-REL-001：发布安装与 OTA 更新验收

| 字段 | 内容 |
| --- | --- |
| 案例名称 | 发布安装与 OTA 更新验收 |
| 优先级 | P0 |
| 前置条件 | 待发布版本已完成签名、notarization 和 updater artifacts 生成；`latest.json` 已发布到 OTA manifest endpoint；本机已安装当前线上旧版本客户端；准备一个包含 workspace、thread 历史、settings 和未提交 Git 改动的真实测试数据集 |

| 步骤 | 操作 | 预期结果 |
| --- | --- | --- |
| 1 | 打开发布产物目录并核对安装包、签名文件、`latest.json`、`release-notes.md` | 必要产物齐全；`latest.json.platforms.*.url` 指向本次版本目录；每个平台产物都有对应签名 |
| 2 | 在干净测试账号或干净应用数据目录中安装待发布版本 | 安装成功；macOS 不提示应用损坏；首次启动无白屏、崩溃或权限异常 |
| 3 | 在全新安装客户端中添加测试 workspace 并创建一个 thread | Workspace、thread、消息发送和基础输出正常 |
| 4 | 卸载全新安装客户端但保留或清理数据后再次安装 | 行为符合预期的数据保留策略；应用可再次启动 |
| 5 | 启动当前线上旧版本客户端 | 旧版本可正常打开；已有 workspace、thread、settings 和 Git 状态展示正常 |
| 6 | 在旧版本客户端执行一次基础任务并修改一个 setting | 旧版本数据准备完成，可用于升级后对比 |
| 7 | 使用应用内 `Check for Updates...` 检查更新 | 应用发现待发布版本；版本号、更新说明和下载入口展示正确 |
| 8 | 执行更新下载和安装 | 下载进度正常；无签名校验失败、下载失败或安装失败 |
| 9 | 按提示重启或重新打开应用 | 应用启动为待发布版本；版本号与 `package.json`、`src-tauri/tauri.conf.json`、`src-tauri/Cargo.toml` 声明一致 |
| 10 | 检查升级前已有 workspace、thread 历史、隐藏状态、图片附件、生图记录、settings 和面板尺寸 | 关键用户数据全部保留；无重复 workspace、丢失历史、设置回退或面板异常 |
| 11 | 在升级后的客户端继续旧 thread 发送 follow-up | 消息成功发送；输出流、完成态和后续交互正常 |
| 12 | 在升级后的客户端执行一次生图或图片附件任务 | 图片预览、资产保存、历史恢复链路正常 |
| 13 | 检查 Git 状态和文件 diff | 分支、未提交变更、diff 内容与本地 Git 结果一致 |
| 14 | 断网后再次触发检查更新 | 应用展示明确网络错误或无更新状态，不崩溃，不影响主功能 |
| 15 | 恢复网络后重启应用 | 应用正常启动；升级后数据仍可恢复 |

| 通过标准 |
| --- |
| 全新安装和旧版本 OTA 升级均可完成；签名、notarization、manifest、release notes、版本号和用户数据迁移均符合预期；升级后 P0 主链路可继续使用；不存在崩溃、白屏、签名校验失败、数据丢失或版本错配 |

## UAT-FULL-001：全功能主链路验收

| 字段 | 内容 |
| --- | --- |
| 案例名称 | 全功能主链路验收 |
| 优先级 | P0 |
| 前置条件 | UAT 环境可用；测试 workspace 存在；Git 仓库包含未提交改动；Codex 执行环境可用；生图能力可用 |

| 步骤 | 操作 | 预期结果 |
| --- | --- | --- |
| 1 | 启动 CodexMonitor | 应用正常打开，无白屏、崩溃或启动错误 |
| 2 | 查看初始页面 | Workspace 列表、主界面、空状态或历史数据展示正常 |
| 3 | 添加有效 workspace | 添加成功，路径、名称、状态显示正确 |
| 4 | 添加重复 workspace | 系统阻止重复添加或给出明确提示 |
| 5 | 添加不存在路径 | 系统展示错误提示，不写入无效 workspace |
| 6 | 切换到有效 workspace | 当前 workspace 被正确选中，thread 区域刷新正确 |
| 7 | 创建新 thread | 新 thread 出现在列表中，状态为可交互 |
| 8 | 发送文本任务 | 消息成功进入 thread，状态进入处理中 |
| 9 | 观察 agent 输出流 | 输出持续更新，无卡死、重复、乱序或明显丢失 |
| 10 | 等待任务完成 | thread 状态变为完成或可继续交互，最终结果展示完整 |
| 11 | 发送 follow-up 任务 | follow-up 成功发送，并关联到当前 thread |
| 12 | 切换 Queue/Steer 设置后再次发送 | 行为符合当前模式设置 |
| 13 | 发送图片附件消息 | 图片预览正常，消息发送成功，历史中可查看附件 |
| 14 | 发送生图任务 | 生图卡片进入生成中状态，完成后展示图片预览 |
| 15 | 打开生成图片 | 图片可正常查看，路径或资源引用有效 |
| 16 | 检查 Git 状态 | 当前分支、变更文件、diff/stage 状态展示正确 |
| 17 | 修改 Settings 配置项 | 设置保存成功，UI 状态立即生效 |
| 18 | 隐藏一个 thread | 被隐藏 thread 不再出现在可见列表中 |
| 19 | 重启应用 | workspace、thread、隐藏状态、图片历史和 settings 正确恢复 |
| 20 | 模拟失败任务后再次发送正常任务 | 失败状态清晰展示；后续任务可继续执行 |

| 通过标准 |
| --- |
| 所有 P0 步骤通过；无崩溃、白屏、数据丢失、状态卡死；文本、图片附件、生图和 Git 展示均符合预期 |

## UAT-WS-001：Workspace 管理验收

| 步骤 | 操作 | 预期结果 |
| --- | --- | --- |
| 1 | 添加有效 workspace | 添加成功并出现在 workspace 列表中 |
| 2 | 切换 workspace | 选中状态、thread 列表和相关面板正确刷新 |
| 3 | 添加重复路径 | 系统阻止重复添加或展示明确提示 |
| 4 | 添加不存在路径 | 系统展示错误提示，不污染持久化数据 |
| 5 | 添加无权限路径 | 系统展示权限错误，不崩溃 |
| 6 | 删除或移除测试 workspace | workspace 从列表移除，其他 workspace 不受影响 |

## UAT-THREAD-001：Thread 创建、恢复与隐藏验收

| 步骤 | 操作 | 预期结果 |
| --- | --- | --- |
| 1 | 在有效 workspace 中创建 thread | thread 创建成功并可交互 |
| 2 | 发送文本任务并等待完成 | 历史消息和最终输出展示完整 |
| 3 | 发送 follow-up 形成连续对话 | follow-up 关联到当前 thread |
| 4 | 隐藏当前 thread | thread 从可见列表移除 |
| 5 | 刷新或重启应用 | 隐藏 thread 不应错误复活 |
| 6 | 恢复可见 thread 并查看历史 | 历史消息和状态完整保留 |

## UAT-COMPOSER-001：Composer 发送与 Follow-up 模式验收

| 步骤 | 操作 | 预期结果 |
| --- | --- | --- |
| 1 | 输入普通文本任务并发送 | 发送成功，输入框状态正确变化 |
| 2 | 任务处理中继续输入 follow-up | Queue 模式下进入队列；Steer 模式下按引导行为处理 |
| 3 | 发送空消息 | 系统阻止提交或给出明确提示 |
| 4 | 发送超长消息 | 系统可发送或给出明确限制提示 |
| 5 | 失败后重新发送 | 新消息可继续发送，不被旧失败状态阻断 |

## UAT-IMG-ATTACH-001：图片附件发送与历史展示验收

| 字段 | 内容 |
| --- | --- |
| 案例名称 | 图片附件发送与历史展示验收 |
| 优先级 | P0 |
| 前置条件 | 当前 workspace 可写；准备 PNG、JPEG、HEIC 图片；至少一张图片路径包含空格 |

| 步骤 | 操作 | 预期结果 |
| --- | --- | --- |
| 1 | 通过文件选择器添加 PNG 图片 | 图片出现在 composer 附件区域，缩略图正常 |
| 2 | 拖拽 JPEG 图片到 composer | 图片成功添加，无重复异常 |
| 3 | 粘贴 HEIC 或系统支持的图片 | 图片成功添加或展示明确不支持提示 |
| 4 | 随消息发送图片附件 | 消息发送成功，图片与文本关联正确 |
| 5 | 点击历史消息中的图片 | 图片可打开预览，无破图或路径错误 |
| 6 | 重启应用后回到 thread | 图片附件仍可查看 |
| 7 | 删除未发送附件 | 附件从 composer 移除，不影响已发送历史 |

## UAT-IMG-GEN-001：生图功能验收

| 字段 | 内容 |
| --- | --- |
| 案例名称 | 生图功能验收 |
| 优先级 | P0 |
| 前置条件 | 生图能力已启用；模型配置可用；网络可用；账号或企业 AI 登录状态满足调用要求 |

| 步骤 | 操作 | 预期结果 |
| --- | --- | --- |
| 1 | 进入测试 workspace 并创建新 thread | thread 可正常交互 |
| 2 | 输入正常生图提示词并发送 | 生图任务成功发起，卡片进入生成中状态 |
| 3 | 等待生成完成 | 返回图片结果，UI 显示完成状态和图片预览 |
| 4 | 打开生成图片 | 图片可查看，无破图、空白或路径错误 |
| 5 | 检查生成资产记录 | 生成图片存在于应用管理的 generated-image library，元数据包含 prompt、model、size、assetId 或 savedPath |
| 6 | 重启应用后回到 thread | 生图历史、图片预览和相关元数据仍可查看 |
| 7 | 发送高复杂度生图提示词 | 系统正常处理；若失败，错误原因明确 |
| 8 | 输入空提示词 | 系统阻止提交或展示明确错误提示 |
| 9 | 模拟未登录、无权限、计费未启用或模型不可用 | 系统展示可理解错误，不崩溃 |
| 10 | 模拟网络失败或服务无返回图片 | 系统展示失败状态和错误原因，不阻塞当前 thread |
| 11 | 失败后重新发送生图任务 | 新任务可继续执行，结果不串线、不挂到错误 thread |
| 12 | 在不同 workspace 中执行生图 | 生成记录归属和展示正确，不污染其他 thread 历史 |

| 通过标准 |
| --- |
| 生图任务可发起、可展示生成中、可完成预览、可保留资产、可重启恢复；失败状态清晰，后续任务不受影响 |

## UAT-GIT-001：Git 状态展示验收

| 步骤 | 操作 | 预期结果 |
| --- | --- | --- |
| 1 | 打开包含 Git 仓库的 workspace | Git 面板展示当前分支和仓库状态 |
| 2 | 查看未提交变更 | 文件列表、状态、数量与真实仓库一致 |
| 3 | 打开 diff | diff 内容与本地 Git 结果一致 |
| 4 | 切换到非 Git 目录 | 展示初始化提示或明确空状态 |
| 5 | 修改文件后刷新状态 | Git 状态可更新，无陈旧数据 |

## UAT-SET-001：Settings 保存与重启恢复验收

| 步骤 | 操作 | 预期结果 |
| --- | --- | --- |
| 1 | 打开 Settings | 设置页面正常显示，当前值正确 |
| 2 | 修改 composer follow-up 模式 | 设置保存成功并立即影响发送行为 |
| 3 | 修改模型或运行时相关配置 | 配置保存成功，错误输入有明确提示 |
| 4 | 重启应用 | 设置恢复为修改后的值 |
| 5 | 恢复默认值 | 默认配置正确生效 |

## UAT-RPC-001：Daemon/RPC Parity 验收

| 步骤 | 操作 | 预期结果 |
| --- | --- | --- |
| 1 | 启动 daemon 或连接远程运行环境 | 连接成功，状态清晰 |
| 2 | 通过 daemon 创建 workspace/thread | 行为与本地 app 主链路一致 |
| 3 | 执行文本任务 | 状态、输出和错误事件与本地链路一致 |
| 4 | 执行生图相关请求 | 若 daemon 支持该能力，返回形态与 app 侧一致；若不支持，错误提示明确 |
| 5 | 断开并重连 | 状态可恢复，不出现重复 thread 或丢失历史 |

## UAT-ERR-001：异常恢复验收

| 步骤 | 操作 | 预期结果 |
| --- | --- | --- |
| 1 | 使用无效 workspace 路径 | 展示明确错误，不写入无效状态 |
| 2 | 在任务执行中模拟命令失败 | thread 展示失败状态和错误信息 |
| 3 | 模拟网络断开后发送任务 | 展示网络或服务错误，不崩溃 |
| 4 | 模拟生图服务失败 | 生图卡片展示失败状态，后续任务可继续 |
| 5 | 重启应用 | 已完成历史保留；失败状态不导致应用卡死 |
| 6 | 恢复网络或配置后重试 | 新任务可正常执行 |

## 上线判定表

| 项目 | 结果 | 备注 |
| --- | --- | --- |
| 发布安装与 OTA 是否通过验收 | 是 / 否 |  |
| P0 案例是否全部通过 | 是 / 否 |  |
| 是否存在 Blocker 缺陷 | 是 / 否 |  |
| 是否存在 Critical 缺陷 | 是 / 否 |  |
| P1 缺陷是否完成风险评估 | 是 / 否 |  |
| 生图功能是否通过验收 | 是 / 否 |  |
| 图片附件是否通过验收 | 是 / 否 |  |
| 重启恢复是否通过验收 | 是 / 否 |  |
| 是否建议上线 | 是 / 否 |  |
| 验收负责人 | 待填写 |  |
| 验收日期 | 待填写 |  |

## 测试执行记录

| 案例编号 | 案例名称 | 优先级 | 执行结果 | 缺陷编号 | 备注 |
| --- | --- | --- | --- | --- | --- |
| UAT-REL-001 | 发布安装与 OTA 更新验收 | P0 | Pass / Fail |  |  |
| UAT-FULL-001 | 全功能主链路验收 | P0 | Pass / Fail |  |  |
| UAT-WS-001 | Workspace 管理验收 | P0 | Pass / Fail |  |  |
| UAT-THREAD-001 | Thread 创建、恢复与隐藏验收 | P0 | Pass / Fail |  |  |
| UAT-COMPOSER-001 | Composer 发送与 Follow-up 模式验收 | P0 | Pass / Fail |  |  |
| UAT-IMG-ATTACH-001 | 图片附件发送与历史展示验收 | P0 | Pass / Fail |  |  |
| UAT-IMG-GEN-001 | 生图功能验收 | P0 | Pass / Fail |  |  |
| UAT-GIT-001 | Git 状态展示验收 | P1 | Pass / Fail |  |  |
| UAT-SET-001 | Settings 保存与重启恢复验收 | P1 | Pass / Fail |  |  |
| UAT-RPC-001 | Daemon/RPC parity 验收 | P1 | Pass / Fail |  |  |
| UAT-ERR-001 | 异常恢复验收 | P1 | Pass / Fail |  |  |

## 验收结论

| 角色 | 姓名 | 结论 | 日期 |
| --- | --- | --- | --- |
| 测试负责人 |  | 通过 / 不通过 |  |
| 产品负责人 |  | 通过 / 不通过 |  |
| 研发负责人 |  | 通过 / 不通过 |  |
| 发布负责人 |  | 准许上线 / 暂缓上线 |  |

最终上线结论以 P0 案例全部通过、无 Blocker/Critical 缺陷遗留、P1 风险可接受为准。
